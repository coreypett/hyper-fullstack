git_url(ENV.fetch("MATCH_GIT_URL"))
storage_mode("git")

type("development")
app_identifier([ENV.fetch("APP_IDENTIFIER", "org.coreypett.fullstack.orderbook")])
team_id(ENV.fetch("FASTLANE_TEAM_ID"))
username(ENV["FASTLANE_USER"])

readonly(ENV.fetch("MATCH_READONLY", "true") == "true")
