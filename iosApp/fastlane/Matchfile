git_url(ENV.fetch("MATCH_GIT_URL"))
storage_mode("git")

type("development")
app_identifier([ENV.fetch("APP_IDENTIFIER", "org.coreypett.fullstack")])
team_id(ENV.fetch("FASTLANE_TEAM_ID"))

readonly(ENV.fetch("MATCH_READONLY", "true") == "true")
